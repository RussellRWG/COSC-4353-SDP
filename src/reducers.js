/*import {REGISTER_PROFILE} from './constants.js';

const initialRegistrationState = {
	username: '',
}

export const registerProfile = (state=initialRegistrationState, action={}) => {
	switch(action.type){
		case REGISTER_PROFILE:
			return Object.assign({}, state, {registrationProfile: action.payload});
		default:
			return state;
	}
}*/